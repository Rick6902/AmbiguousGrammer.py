This is Python Programme code to check whether a given language or grammar is ambiguous or not.
Simply provide a grammar as input in the python source code, and run it.
It will open a tkinter window where you can click the button for "Check Ambiguity" and print out the result.
